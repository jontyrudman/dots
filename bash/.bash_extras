#
# Source this in a .bashrc after moving it and .bash_prompt to $HOME.
#

source $HOME/.bash_prompt
export PATH="$HOME/.node_modules/bin:$HOME/.local/bin:$PATH"
export EDITOR=nvim

[ -f ~/.fzf.bash ] && source ~/.fzf.bash
